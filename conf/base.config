/*
 * ProSIFT -- conf/base.config
 * Default resource allocations per process. Override in profiles or
 * per-process withName blocks as needed.
 */

process {

    // Global defaults
    cpus   = 1
    memory = '4.GB'
    time   = '2.h'

    errorStrategy = { task.exitStatus in [143, 137, 104, 134, 139] ? 'retry' : 'finish' }
    maxRetries    = 2
    maxErrors     = '-1'

    // Per-process resource overrides (populated as processes are implemented)
    // withName: 'VALIDATE_INPUTS' {
    //     cpus   = 1
    //     memory = '8.GB'
    // }

}
